import React, { Component } from "react";
import PropTypes from "prop-types";

class test extends Component {
  static propTypes = {};

  state = {
    test: "test",
  };

  render() {
    return <div></div>;
  }
}

export default test;
